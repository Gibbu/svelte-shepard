- Fully impliment `navigate` function.
- Nested routes
- Disable SPA routing with data attribute.
- Expose "View" component on nested routes.
  > Get current page, if view found render component. Props can be passed down?
